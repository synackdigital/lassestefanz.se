jQuery(window).load(function() {
    $ = jQuery;

    /* Instagram feed */
    $('.instagram-feed').flexslider({
        animation: "slide",
        animationLoop: true,
        itemWidth: 269,
        itemMargin: 5
    });

    $('#hero .flexslider').flexslider({

    });
});
